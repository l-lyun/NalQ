# Public base URL used by the browser application.
VITE_API_BASE_URL=http://localhost:8080

# Enable only after the quiz generation, self-assessment, and review APIs are deployed.
VITE_QUIZ_API_ENABLED=false
