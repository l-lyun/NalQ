# Public base URL used by the browser application.
VITE_API_BASE_URL=http://localhost:8080

# Normal development and production use the real quiz API.
# Use `pnpm dev:mock` only when reviewing fixture screens without a server.
VITE_QUIZ_RUNTIME_MODE=api
