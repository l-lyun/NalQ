# Public base URL used by the browser application.
VITE_API_BASE_URL=http://localhost:8080

# In local development, false uses explicit in-browser fixtures for the complete quiz flow.
# Set true only when the quiz generation, self-assessment, and review APIs are available.
# Production never falls back to fixture data.
VITE_QUIZ_API_ENABLED=false
